#ifndef EVENT_H
#define EVENT_H

typedef enum {
	EVENT_CURSOR_MOVE,
	EVENT_CUSROR_VISIBILITY_CHANGE,

	EVENT_BUFFER_INSERT,
	EVENT_BUFFER_CHANGE,

	EVENT_MODE_CHANGED,
	EVENT_COMMAND_EXECUTED,

	EVENT_KEY_PRESSED,

	EVENT_SCREEN_RESIZE,
	EVENT_REDRAW,
} event;

#endif
